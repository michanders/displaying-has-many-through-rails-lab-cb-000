<%@patient.appointments.each do |app|%>
  Appointment at: <%=link_to app.appointment_datetime.strftime("%B %d, %Y at %H:%M"), appointment_path(app) %> with <%= link_to app.doctor.name, doctor_path(app.doctor) %>
<%end%>
